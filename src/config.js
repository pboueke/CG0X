// config.js stores all the configuration and global variables

var current_scene = "welcome_area";
var next_scene = "main_menu";

var sound_options = {
                     sfx_enabled: true,
                     music_enabled: true
                    };

// ball_type values: raditional, gravitational, curveball
var ball_type = "traditional";
